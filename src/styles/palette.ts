/**
 * 와이어프레임에서 그대로 옮긴 중성색이다. 판정과 미디어 조작 단계의 색은
 * 아직 정해지지 않았고(U-03·U-04) 이 계열 안에서만 쓴다.
 *
 * 컴포넌트는 이 값을 직접 쓰지 않고 `contract.css.ts`의 이름을 쓴다.
 */
export const gray = {
  0: '#ffffff',
  25: '#faf9f6',
  50: '#f7f5f1',
  100: '#f2efe9',
  150: '#efece5',
  200: '#e8e5df',
  250: '#e3dfd8',
  300: '#e2ded6',
  350: '#ece9e3',
  400: '#d5d1c9',
  450: '#c6c2b9',
  500: '#b8b4ac',
  550: '#a8a49b',
  600: '#9b968d',
  650: '#8c8880',
  700: '#6f6c66',
  800: '#3d3b37',
  900: '#1c1b19',
} as const

/**
 * 참새 표식에만 쓰는 색이다. 본문 중성색은 위 `gray`를 그대로 둔다.
 * 판정과 미디어 조작 단계의 색은 아직 정해지지 않았고 이 값과 무관하다.
 */
export const brand = {
  /** 표식의 먹색. 본문 먹색보다 아주 조금 차다. */
  ink: '#1a1a1a',
  /** 아이콘 타일과 브라우저 상단 띠. */
  cream: '#f6e6cf',
  /** 부리. 표식에서 유일한 유채색이다. */
  beak: '#f2b233',
  /** 카드 바탕처럼 넓은 면에 쓰는 갈색. */
  deep: '#a8703b',
} as const

/**
 * 판정과 미디어 조작 단계의 색이다. 축마다 계열을 달리해 두 축이 한 축으로
 * 읽히지 않게 한다. 색을 지워도 문구와 아이콘으로 구분된다.
 */
export const signal = {
  /** 근거와 일치. 유일하게 괜찮다고 말하는 자리다. */
  supported: '#2f7d55',
  /** 근거와 불일치. */
  refuted: '#c0392b',
  /** 조작 의심. 판정과 다른 계열이어야 두 축이 섞이지 않는다. */
  suspected: '#c2410c',
} as const
